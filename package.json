{
  "name": "map-probability",
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive world heatmap showing birth probability by country and continent",
  "scripts": {
    "dev": "node server.js",
    "test": "node --test --experimental-vm-modules '**/*.test.js'",
    "lint": "eslint .",
    "ingest": "node scripts/ingest.js"
  },
  "dependencies": {
    "sql.js": "^1.11.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
